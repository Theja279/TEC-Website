# TEC-Website Frontend UI Task

## Plan Steps

1. Update global styling: fonts (Poppins/Inter), design tokens (Navy/Gold/Light Gray), base background/text. ✅

2. Update `app/layout.tsx`: TEC metadata, replace Geist fonts with Poppins/Inter, ensure global classes. ✅

3. Build reusable components in `frontend/components/`:
   - Container, Button, Navbar, Footer, Hero, SectionTitle, Cards, Statistics, CTASection,
     Testimonials, FAQAccordion, ContactForm.
4. Build section components for each page (so every section is independently reusable).
5. Create pages in `frontend/app/` for:
   - Home, About, Financepreneur, Customer, Partner, Founder Story, Success Stories, FAQ, Contact.
6. Integrate Framer Motion animations (fade/slide/hover) and smooth scrolling.
7. Ensure responsive/mobile-first layout and accessibility.
8. Run `npm run lint` and `npm run build` to validate.
